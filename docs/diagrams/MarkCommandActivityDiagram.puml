@startuml
skin rose
skinparam ActivityFontSize 15
skinparam ArrowFontSize 12
start
:User enters "mark" command with indices;
:Logic parses arguments into person and lesson indices;
:Create and execute MarkCommand;
if (Valid person index?) then (no)
  :Display invalid person index error;
  stop
else (no)
  :Retrieve target person;
endif
if (Valid lesson index?) then (no)
  :Display invalid lesson index error;
  stop
else (yes)
  :Retrieve target lesson;
endif
if (Lesson already marked?) then (yes)
  :Display "already marked" error;
  stop
else (no)
  :Update lesson attendance in model;
  :Format success message;
  :Display success to user;
  stop
endif
@enduml